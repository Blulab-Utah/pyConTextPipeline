from setuptools import setup

setup(name='RESplit',
      version='1.0.0',
      description='Sentence segmenter based on regular expressions for section, sentences, and a fallback',
      url='https://gitlab.chpc.utah.edu/endoqual/endoscopy_path_extract',
      author='Garrett Cole',
      author_email='garrett.cole@hsc.utah.edu',
      license='Apache 2.0',
      packages=[''],
      install_requires=[''],
      zip_safe=False)
